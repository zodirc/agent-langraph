# Common Knowledge

通用知识库目录（`domain=common`）。

建议放置：

- 运行时通用约束
- 安全与权限策略
- 与具体任务模式无关的流程说明

说明：

- 当前内置 common 文档主要通过 `app/services/knowledge_seed.py` 写入（非 markdown 文件）。
- 后续如果要把 common 文档文件化，可在此目录新增 `.md` 并在 seed 中注册。
