# 代码编辑模式指南

适用场景：代码修复、重构、功能新增、测试补齐。

1. 先最小改动，再扩大范围；优先保持行为兼容。
2. 每次修改都要能解释“为什么改”，避免无目的风格化重排。
3. 对风险操作（删除、批量替换、路径改写）先 dry-run 或给出预览。
4. 优先补充或更新对应测试，再进行回归验证。
5. 输出建议以可执行步骤为主，避免模糊描述。
6. 涉及安全边界（路径、权限、注入）时，默认收紧策略并显式审计。
