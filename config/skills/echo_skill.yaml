skill_id: echo_helper
name: Echo Helper
version: "1.0.0"
description: Wraps the built-in echo tool as a declarative skill (legacy invoke demo).
summary: Echo demo for skill-as-tool compatibility.
source_type: builtin
status: published
visibility: system_public
base_domain: single_turn
category: general
required_role: user
risk_level: LOW
tags:
  - general
  - qa
enabled: true
deprecated: false
tool_name: echo
input_schema:
  type: object
  properties:
    message:
      type: string
  required:
    - message
